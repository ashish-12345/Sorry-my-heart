<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>For You ❤️</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #ffeef1;
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
    }

    .container {
      position: relative;
      max-width: 400px;
      margin: 40px auto;
      box-shadow: 0 0 20px rgba(0,0,0,0.2);
      border-radius: 20px;
      overflow: hidden;
    }

    img {
      width: 100%;
      display: block;
    }

    button {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      padding: 12px 24px;
      font-size: 16px;
      background-color: #ff4b5c;
      color: white;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }

    .hearts {
      font-size: 24px;
      margin-top: 10px;
      animation: float 6s infinite ease-in-out;
    }

    @keyframes float {
      0% { opacity: 0; transform: translateY(100px); }
      50% { opacity: 1; transform: translateY(0); }
      100% { opacity: 0; transform: translateY(-100px); }
    }

    /* Modal styles */
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: auto;
      background-color: rgba(0,0,0,0.5);
    }

    .modal-content {
      background-color: white;
      margin: 15% auto;
      padding: 30px;
      border-radius: 20px;
      max-width: 300px;
      text-align: center;
      color: #ff4b5c;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
      animation: popIn 0.4s ease;
    }

    @keyframes popIn {
      from { transform: scale(0.8); opacity: 0; }
      to { transform: scale(1); opacity: 1; }
    }

    .close-btn {
      margin-top: 20px;
      background-color: #ff4b5c;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 20px;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <div class="hearts">💖 💕 💘 💝 💓</div>

  <div class="container">
    <button onclick="showModal()">Click Here</button>
  </div>

  <!-- Modal -->
  <div id="popupModal" class="modal">
    <div class="modal-content">
      <h2>Good Morning ☀️</h2>
      <p>Sorry my love ❤️<br>Just wanted to see you smile 💌</p>
      <button class="close-btn" onclick="closeModal()">Aww 🥺</button>
    </div>
  </div>

  <script>
    function showModal() {
      document.getElementById('popupModal').style.display = 'block';
    }

    function closeModal() {
      document.getElementById('popupModal').style.display = 'none';
    }
  </script>

</body>
</html>
